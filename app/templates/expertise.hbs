{{#transition-group transition-class="SlideIn"}}

		<h1>{{model.title}}</h1>
		<h2>{{model.intro}}</h2>
		<hr> 

	{{!-- {{#each model.skills as |item|}}
		{{log item}}
		{{#each item.qualities as |list1| }}
			{{list1}}
		{{/each}}
	{{/each}} --}}

	{{numbered-blocks}} 
{{/transition-group}}